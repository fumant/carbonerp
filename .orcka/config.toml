[project]
name = "carbon"
language = "typescript"

[commands]
test = "npm test"
lint = "npm run lint"
build = "npm run build"

[engine]
name = "opencode"
permission = "full-auto"

[server]
port = 4096
hostname = "127.0.0.1"

[rules]
items = []

[boundaries]
never_touch = []

